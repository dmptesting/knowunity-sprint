import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { SpacingFoundations } from './SpacingFoundations';

<Meta title="Foundations/Spacing" />

# Spacing

Every `Space` token, rendered as a bar at its real pixel width (capped visually at 160px, the largest real value)
so the scale reads at true relative size. `Space` tokens have no `$description` field in tokens.json, which is
called out on each row rather than left blank.

<Unstyled>
  <SpacingFoundations />
</Unstyled>
